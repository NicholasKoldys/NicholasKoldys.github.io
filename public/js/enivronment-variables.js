const cssLink = document.getElementById("style-sheet"); // * body-init

const header = document.getElementById("header");
const menuBtn = document.getElementById("burger-menu");
const menuList = document.getElementById("menu-list");

const asideNav = document.getElementById("aside-nav");
const timeline = document.querySelectorAll(".color-timeline-segment");
const sectionTitles = document.querySelectorAll(".section-title");

const dynNav = document.getElementById("dyn-nav");
const collapsedSegTemplate = document.getElementById("collapsed-segment-template");

const sections = document.querySelectorAll("section");